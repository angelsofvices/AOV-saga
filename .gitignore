# Local pre-edit snapshots — never deploy these
backups/

# OS / editor cruft
.DS_Store
Thumbs.db

# Legacy oversized videos — stay ignored
assets/rp7 intro vid v2.mp4
video/title-intro.mp4

# Ignore all MP4s by default but allow the short intro that mobile needs
*.mp4
!video/intro-v2.mp4
!video/dream.mp4
